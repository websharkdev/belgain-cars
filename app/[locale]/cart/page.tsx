import { DemoPlaceholderPage } from '@/components/layout/demo-placeholder-page';

export default function CartPage() {
  return (
    <DemoPlaceholderPage
      eyebrow="Cart preview"
      title="Your cart is ready for checkout UI"
      description="The header action now routes to a real page instead of a dead button."
      actionLabel="Continue shopping"
    />
  );
}
